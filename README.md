# JaCoCoReporter
A txt report generator for JaCoCo binaries

Branches roadmap:
* alfa-1 - First operational version. Still needs dealing with exceptions better
* alfa-2 - Tries to substitute alfa-1's O(n^4) loop with a n-ary tree with a variable n